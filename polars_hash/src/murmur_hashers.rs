// use fasthash::{murmur, MurmurHasher, FastHasher};
// use std::fmt::Write;

// pub fn murmur32_hash(value: &str, output: &mut u32) {
//     let hash = murmur::hash32(value.as_bytes());
//     write!(output, "{:x}", hash).unwrap()
// }