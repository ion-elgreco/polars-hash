# This file is autogenerated by maturin v0.14.17
# To update, run
#
#    maturin generate-ci github -m polars_hash/Cargo.toml --platform all
#
name: CI

on:
  merge_group:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  build:
    # A calling job caps the called workflow's job permissions.
    permissions:
      contents: read
    uses: ./.github/workflows/build.yml
